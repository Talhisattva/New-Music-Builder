# References

- `_references/figma_export/` is reference-only material exported from Figma.
- It must not be treated as production code for New Music Builder.
- `Builder Test.zip` remains the original export artifact.
- The Python app in `src/new_music_builder/` is the implementation target.